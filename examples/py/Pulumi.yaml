name: pulumi-descope-py
runtime:
  name: python
  options:
    virtualenv: venv
description: A minimal Python Pulumi program that uses the Pulumi descope package.
config:
  pulumi:tags:
    value:
      pulumi:template: python
